using System;

class Program
{
    static void Main()
    {
        int n = 7;
        bool isPrime = true;

        for (int i = 2; i <= n/2; i++)
        {
            if (n % i == 0)
            {
                isPrime = false;
                break;
            }
        }

        Console.WriteLine(isPrime ? "Prime" : "Not Prime");
    }
}
